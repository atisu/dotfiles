HOMEDIR=$HOME
TARGET=.sshagent

#ssh-agent > /tmp/$TARGET
#head -n 2 /tmp/$TARGET > $HOMEDIR/$TARGET
#rm -f /tmp/$TARGET
#. $HOMEDIR/$TARGET
for i in id_ed25519_ntt.pem id_ed25519 id_rsa azure_ntt.pem atisu_elkh_os_arnl; do
  ssh-add $HOMEDIR/.ssh/$i
done
